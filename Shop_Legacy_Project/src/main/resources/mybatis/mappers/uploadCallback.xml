<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shoplegacy.file.mapper.FileUploadMapper">

    <insert id="insertCallback">
        insert into upload_callback_log (result_code, err_msg, ordnum)
        values (#{resultCode}, #{errMsg}, #{ordnum})
    </insert>

</mapper>
