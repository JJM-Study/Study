<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shoplegacy.file.mapper.FileUploadMapper">

    <insert id="insertUploaded"> <!-- 일단 아이템 바구니 담기 구현 전까지만 전체 파라미터 담고, 이후엔 file 업로드 관련 정보만 남길 것. -->
        INSERT INTO order_cart (
        cart_no, user_id, prod_no, prod_name, ord_no, crt_dt, upd_dt, del_yn, file_url, file_name, callback_url
        )
        VALUES (
        #{cartNo}, #{userId}, #{prodNo}, #{prodName}, #{ordNo}, #{crtDt}, #{updDt}, #{delYn}, #{fileUrl}, #{fileName}, #{callbackUrl}
        )
    </insert>

</mapper>
